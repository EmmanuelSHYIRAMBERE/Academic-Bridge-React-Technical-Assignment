// project import
import {
  dashboard,
  messages,
  documents,
  projects,
  analytics,
  teamMember1,
  teamMember2,
  addTeamMember,
  settings,
  profile
} from './dashboard';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, messages, documents, projects, analytics, teamMember1, teamMember2, addTeamMember, settings, profile]
};

export default menuItems;
